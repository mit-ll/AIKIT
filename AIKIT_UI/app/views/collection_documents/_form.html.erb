<%= form_with(model: collection_document) do |form| %>
  <% if collection_document.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(collection_document.errors.count, "error") %> prohibited this collection_document from being saved:</h2>

      <ul>
        <% collection_document.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
