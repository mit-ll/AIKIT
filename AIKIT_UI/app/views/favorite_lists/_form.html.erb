<%= form_with(model: favorite_list) do |form| %>
  <% if favorite_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(favorite_list.errors.count, "error") %> prohibited this favorite_list from being saved:</h2>

      <ul>
        <% favorite_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :list_name, style: "display: block" %>
    <%= form.text_field :list_name %>
  </div>

  <div>
    <%= form.label :is_public, style: "display: block" %>
    <%= form.check_box :is_public %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
