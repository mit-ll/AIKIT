<%= form_with(model: user_test) do |form| %>
  <% if user_test.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_test.errors.count, "error") %> prohibited this user_test from being saved:</h2>

      <ul>
        <% user_test.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
