<%= form_with(model: user_read) do |form| %>
  <% if user_read.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_read.errors.count, "error") %> prohibited this user_read from being saved:</h2>

      <ul>
        <% user_read.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :page_number, style: "display: block" %>
    <%= form.number_field :page_number %>
  </div>

  <div>
    <%= form.label :is_complete, style: "display: block" %>
    <%= form.check_box :is_complete %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
